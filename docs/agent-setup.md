# Agent setup and private interaction

## Confirm the MCP connection

Stage5 Browser should be registered at user scope when agents need it from multiple repositories. The active local registration points directly to this checkout's `dist/launcher.js`; it is not a cached marketplace-plugin installation and does not need redeployment after a compatible patch.

Ordinary builds and tests run headless and must not interrupt the user's desktop. The macOS native-window activation smoke is different: its purpose is to hide, restore, and foreground a disposable browser, so focus movement is unavoidable for that single test. It is gated behind both `STAGE5_BROWSER_NATIVE_WINDOW_SMOKE=1` and `STAGE5_BROWSER_ALLOW_FOCUS_CHANGE=1`. Enable it only when native activation is the behavior under test, disclose the expected interruption, and do not create a repetitive action-time approval ritual or run the smoke for unrelated changes.

### ChatGPT desktop app

The ChatGPT desktop app and Codex CLI share `~/.codex/config.toml`. The official host documentation is [Model Context Protocol](https://learn.chatgpt.com/docs/extend/mcp?surface=chatgpt-desktop).

From any project directory, first inspect the shared configuration without opening a browser:

```bash
codex mcp list
```

The expected result includes an enabled `stage5_browser` entry whose command resolves to this repository's `dist/launcher.js`.

Use this decision tree:

1. If the current agent exposes the number of Stage5 Browser tools declared at `stage5Browser.toolCount` in `package.json`, call `browser_status`; do not touch host configuration.
2. If `codex mcp list` does not contain `stage5_browser`, the server is not registered for that Codex host. Register it before browser work.
3. If the CLI reports it enabled but the current ChatGPT agent lacks the tools or the app does not show the server, the running app loaded its host configuration before registration. Fully quit and reopen ChatGPT once, then start or resume an agent run. Do not rebuild, reinstall, redeploy, or add a duplicate server.
4. Once connected, `browser_status` should report the version and tool count declared in `package.json`, with `restartRequired: false`.
5. If `compatibleUpdateAvailable` is true, continue normally. A proven native-CDP session rolls forward in place on the next operation. A connected direct-Playwright session defers the worker update until explicit stop so unsaved page state is never reconstructed from a URL.
6. Reconnect the host only when `restartRequired` is true. This means the MCP tool catalog, MCP-to-worker protocol, or MCP-host lifecycle behavior actually changed.

The ChatGPT composer can use `/mcp` to inspect connected servers after the host has loaded them. If the server is missing there while `codex mcp list` says it is enabled, treat that as stale host state—not failed registration.

### Claude Code

From any project directory, Claude Code can verify the registration without opening a browser:

```bash
claude mcp get stage5_browser
```

The expected result includes both:

```text
Scope: User config (available in all your projects)
Status: ✔ Connected
```

Inside an interactive Claude Code session, `/mcp` provides the equivalent connection view. If `ToolSearch` cannot find Stage5 Browser, check the MCP connection first instead of inspecting configuration files or constructing JSON-RPC by hand.

Claude Code builds its MCP tool catalog when the agent process starts. A server registered after a long-running session began will not appear in that session. Exit the process and resume the conversation with:

```bash
claude --continue
```

After reconnection, a natural-language instruction such as “Use the `stage5_browser` MCP tools instead of `claude-in-chrome`” is sufficient. The host exposes the individual tools to the model; no JSON-RPC bridge is necessary.

## Agent Lounge connection

Release 0.12.0 adds an explicit restored temporary-activation policy and bounded content wait to `browser_inspect_tab`. The surface is 32 tools, tool catalog 11, and worker protocol 9. Tasks using an older contract require one host reconnect. Do not reinstall or duplicate the existing `stage5_browser` registration. ChatGPT/Codex should reconnect its MCP host once; Claude Code should exit and resume the same conversation with `claude --continue`.

Release 0.12.1 keeps that tool catalog and protocol unchanged. It is a compatible worker update for one same-tab visibility recovery during temporary inspection and postconditioned guarded keyboard activation of covered native buttons. Existing 0.12.0 hosts should stay connected and call `browser_status` once at a safe boundary; require worker version 0.12.1 and `restartRequired: false`.

Release 0.12.2 also keeps tool catalog 11, worker protocol 9, and 32 tools. It adds bounded ref-free deep detail for visible article/standalone-quotation content and recognizes standalone quotations plus exact generic loading leaves in content waits. Existing 0.12.x hosts should stay connected and call `browser_status` once at a safe boundary; require worker version 0.12.2 and `restartRequired: false`.

Release 0.13.0 expands the public hand to 53 tools, tool catalog 12, and worker protocol 10. It adds exhaustive manager/phase contracts, operation reservation and terminal-status recovery, durable page-replacement risk, exact selected-page activation, history/close/motion primitives, form summaries and staged plans, single/multi-option selection, field-scoped private handoff, expected-dialog handling, durable download capture, and agent-declared review policy. Every host must reconnect once; the existing `stage5_browser` registration already points to this checkout, so do not reinstall or add another registration. Before reconnecting, use the still-loaded 0.12 host only to inspect status and reach a state-safe boundary. A direct-Playwright browser closes during host shutdown and must have no unsaved state. A proven native-CDP Chromium-family browser may remain open; graceful shutdown detaches from that exact owned process, and the new host reattaches through `browser_start`. After reconnect, rejoin the Lounge, require version 0.13.0/catalog 12/protocol 10/53 tools with `restartRequired: false`, call `browser_tabs`, and discard all earlier tab/frame/snapshot/control/form IDs before continuing from fresh observation. Never replay an action that was frozen or had possible input before the reconnect.

Release 0.14.0 keeps catalog 12 and 53 tools, moves to worker protocol 11, and introduces MCP-host behavior version 1. It binds the stable Lounge identity to a privacy-minimized durable context containing only the selected backend and explicit review-policy mode. This fixes a reconnect silently reverting a Chrome agent to the configured Chromium default while another agent legitimately uses Brave. Reconnect once, rejoin the same Lounge identity before browser work, and require 0.14.0/protocol 11/catalog 12/53 tools with `restartRequired: false`. The first 0.14.0 run cannot invent a mapping that 0.13.0 never stored: use `browser_available`, require one uniquely proven intended orphaned profile, and call `browser_start` with that explicit backend once. That successful start seeds future reconnects. Never choose when more than one profile could be the agent's prior context.

Release 0.15.0 expands the surface to 54 tools, catalog 13, and worker protocol 12. It adds bounded priority-first MCP results and `browser_execution_traces`, whose durable privacy-safe records expose the responsible manager, phase transitions, dispatch boundary, reconciliation facts, and terminal outcome by `operationId`. It also makes passive exact-tab inspection use the canonical unique-modal root and reconciles custom single/multi-select effects through selected representation or popup closure without replay. Every host must reconnect once, rejoin the same Lounge identity, and require 0.15.0/protocol 12/catalog 13/54 tools with `restartRequired: false`. Preserve any owned Chromium-family native browser; after reconnect, use `browser_available` and adopt exactly one uniquely proven intended recoverable profile, then take fresh status, tab, page-event, and semantic observations. Discard every old ref/capability and never replay a frozen or possibly dispatched action.

Release 0.15.1 keeps protocol 12, catalog 13, and 54 tools. It durably reconstructs an `awaiting_user` Chromium-family private handoff after a compatible worker or full host restart, including the narrow crash window between writing the native control record and persisting its browser-process lease. Keep the exact private browser open, call `browser_auth_status` once, and require `awaiting_user` plus a recoverable exact owner. Never call `browser_request_login_handoff` again. After the user finishes the private step, call `browser_resume_after_login` once; only that explicit command may claim ownership and attach.

Release 0.15.2 keeps protocol 12, catalog 13, and 54 tools. It makes ordinary exact buttons/options pointer-first, reserves keyboard activation for an explicit postconditioned recovery, scrolls the clipping modal/nested viewport before contact, and retains the exact observed node through page preparation. If that node is replaced, rebinding is first constrained to its privacy-fingerprinted article, table row, or list item. Partial selection reconciliation now emits canonical telemetry checks. This is a compatible worker update: keep the MCP host connected and call `browser_status` once at a safe boundary; require version 0.15.2 with `restartRequired: false`. Never replay a prior partial-input selection. A prior zero-dispatch modal/ref failure may receive one new attempt only after a fresh observation proves the same authorized target and state.

Release 0.15.3 keeps protocol 12, catalog 13, and 54 tools. Exact-ref viewport preparation now plans both axes, follows slot/shadow composed ancestry to the real nested scroll surface, intersects upstream clipping regions, and returns privacy-safe movement evidence. Control reveal now owns its popup-effect reconciliation: structural ownership outranks focused and expanded fallbacks, an already-open exact target popup is inspected without extra input, and a pointer-down/mouse-down opener effect is never replayed. This is a compatible worker update. Adopt it only at a safe boundary, require version 0.15.3 with `restartRequired: false`, discard old refs and control capabilities, and begin with fresh passive state. A frozen page with multiple open popups must use `revealOptions=false` first; proceed only if the exact target popup is uniquely associated, and never replay the opener that created the state.

Release 0.15.4 keeps protocol 12, catalog 13, and 54 tools. When a visible, enabled exact native button cannot enter the viewport because no surface can scroll, the click manager may use exact keyboard reach only if the caller supplied a non-null bounded postcondition; without that postcondition the same target remains a zero-input failure. Viewport telemetry reports `reachStrategy`. Control inspection can passively associate unlinked portal popups through one unique privacy-safe geometric anchor after explicit and structural ownership, while equal anchors fail closed; `reveal.associationProof` exposes only the categorical proof. Adopt at an existing safe boundary, require version 0.15.4 with `restartRequired: false`, and discard old refs/inspection IDs. Never replay an earlier partial opener.

Release 0.15.5 keeps protocol 12, catalog 13, and 54 tools, and changes MCP host behavior to 2. Reconnect the MCP host once, rejoin the same stable Lounge identity, and require version 0.15.5 with `hostBehaviorVersion: 2` and `restartRequired: false`; then discard all old refs and inspection IDs. Control inspection now discovers generic positioned/popover/dialog option surfaces through the same bounded source of truth as semantic popups and returns categorical `surfaceProof` plus `renderedPopupCount`. Exact-target actionability may override a false overflow-clipping inference only when a bounded browser hit test inside the target's own client rectangles reaches that exact target or a composed-tree descendant. Execution traces retain those categorical proofs and target state without labels, selectors, values, content, or coordinates.

Release 0.15.6 is a compatible worker update that keeps host behavior 2, protocol 12, catalog 13, and 54 tools. A broad positioned portal is partitioned only when strict option branches are separated by a bounded real gap; contiguous option wrappers remain one surface. `expectedHidden` now succeeds when every match in the bounded exact semantic candidate set is hidden, while any visible match, an over-limit set, or observation failure still fails closed. Keep the MCP host connected, call `browser_status` at the existing safe boundary, require worker version 0.15.6 with `restartRequired: false`, and discard old refs/inspection IDs. Never replay the action that produced the current state.

Release 0.15.7 is another compatible worker update with host behavior 2, protocol 12, catalog 13, and 54 tools unchanged. Popup ownership no longer treats every control inside a broad plausibility threshold as equally ambiguous: within one non-structural proof tier, it uses normalized edge gap and resolves only a candidate with a decisive lead. Structural conflicts and exact/near positional ties remain zero-input ambiguity. Keep the MCP host connected, adopt 0.15.7 through `browser_status` at the existing safe boundary, require `restartRequired: false`, and discard old control capabilities before one separately authorized passive verification.

Release 0.15.8 keeps that host contract unchanged and adds the next positional boundary. In the weakest spatial tier only, a sibling with material rectangle overlap is excluded when a bounded hit test inside that overlap proves the popup surface covers it and the surviving exterior anchor is tightly adjacent. Structural, focused, expanded, uncovered-overlap, and true exterior conflicts still fail closed. Control results and failures expose only the proof tier, bounded candidate-class counts, and decision category; they omit geometry and semantics. Keep the host connected, adopt 0.15.8 through one `browser_status` call at the existing safe boundary, require `restartRequired: false`, discard old capabilities, and perform no live verification unless the coordinator separately releases it.

Release 0.15.9 changes MCP host behavior to 3 while retaining protocol 12, catalog 13, and 54 tools. The new owner categories are extracted and persisted by the long-lived host, so an already-running host cannot learn that privacy allowlist from a worker replacement. Reconnect the MCP host once, rejoin the same stable Lounge identity, and require version 0.15.9 with `hostBehaviorVersion: 3` and `restartRequired: false`; then discard old refs and inspection IDs. Future `browser_execution_traces` retain categorical `popupOwnership`. Never replay an earlier browser operation merely to backfill telemetry; existing trace rows remain immutable.

Release 0.15.10 changes MCP host behavior to 4 while retaining protocol 12, catalog 13, and 54 tools. A custom selection now bounds its exact retained-control baseline before dispatch, and a worker stopped by the outer hard deadline hands completed plus active action-phase state to the host for the exact timed-out request. Reconnect once, rejoin the same stable Lounge identity, require version 0.15.10 with `hostBehaviorVersion: 4` and `restartRequired: false`, and discard all old refs and inspection IDs. A trace marked possible dispatch remains non-retriable; telemetry improves diagnosis but never authorizes replay.

Release 0.15.11 is a compatible worker update that keeps host behavior 4, protocol 12, catalog 13, and 54 tools. Custom selection now retains an adaptively isolated exact field region and can prove a newly rendered selected representation there even when a multi-select popup intentionally stays open. Popup closure alone cannot prove multi-selection, and an unavailable popup observation is unknown rather than closed. Keep the MCP host connected, call `browser_status` at a safe boundary, and require `worker.version: 0.15.11`, `mcp.currentVersion: 0.15.11`, host behavior 4, and `restartRequired: false`; an already-running compatible host may correctly retain `mcp.version: 0.15.10`. Discard old inspection IDs. Never replay a prior trusted click; re-observe authoritative form state under fresh user authority before considering another option.

Release 0.15.12 is compatible with the same host behavior 4, protocol 12, catalog 13, and 54-tool contract. When a retained custom-control opener is definitively detached but the document is unchanged, selection may perform one read-only exact role/name re-resolution, require one newly associated rendered popup, then continue through the normal single dispatch gate. Slow/unknown probes, ambiguity, missing popup state, and document replacement still fail before input. Require worker/current version 0.15.12 at the existing safe boundary and discard old inspection IDs; a compatible host may retain its prior loaded package label. No release notice grants live-account authority, and no historical action may be replayed.

Release 0.15.13 applies the same positional capability rule when the exact control remains connected but its retained popup node is definitively non-rendered. Stage5 may read-only discover one newly rendered uniquely associated popup, records `target_changed_before_input`, and then uses the ordinary exact-option dispatch gate. It never replays the opener; unknown visibility, missing or ambiguous association, and hidden replacement state fail before input. Require worker/current 0.15.13 at the existing safe boundary and discard old inspection IDs. No release notice grants live-account authority.

Release 0.15.14 keeps host behavior 4, protocol 12, catalog 13, and 54 tools unchanged. When a framework replaces an exact control's popup through a short missing interval, the selection observe phase now uses its existing bounded budget for passive re-resolution. Hidden retained surfaces cannot outrank a uniquely owned rendered replacement. A closed control remains a zero-input failure, and Stage5 never replays the opener. Require worker/current 0.15.14 at an existing safe boundary and discard old inspection IDs. No release notice grants live-account authority.

Release 0.15.15 is compatible with the same host contract. Inspection and selection reconciliation now share one exact option-state evaluator that recognizes explicit state on a `role=option` row or bounded descendants, including nested native checkboxes and common framework state carriers. Checkbox state marks a multi-select even when the popup intentionally remains open; appearance-only classes are ignored, conflicts remain unknown, and an already-selected option is never toggled. Require worker/current 0.15.15 at a safe boundary and discard old inspection IDs. Never replay a possibly dispatched historical selection, and perform no live verification without fresh controlling-thread authority.

Release 0.15.16 keeps host behavior 4, protocol 12, catalog 13, and 54 tools unchanged. Custom-control inspection now inventories all unique observed option names against the same adaptively isolated field representation used by reconciliation, in one browser pass. Existing exact chips map to selected options, several mapped choices prove multi-select, unrelated fields and duplicate names are excluded, and a represented option is revalidated through action phases with zero dispatch. Explicit conflicts fail before input. Require worker/current 0.15.16 at a safe boundary and discard old inspection IDs. This release grants no live observation or action and never authorizes replay of a prior selection.

Release 0.16.0 is an intentional reconnect boundary. It changes `browser_select_option` to accept a boolean desired state, moves worker protocol to 13 and catalog to 14, and adds privacy-safe desired/observed option-state booleans under host behavior 5; the tool count remains 54. Reconnect each host once, rejoin its same stable Lounge identity, require MCP/worker/current 0.16.0 with `restartRequired:false`, and discard all old capabilities. `selected` defaults to true. Use `selected:false` only from a fresh inspection reporting the exact option `selected:true`; null/conflict is a no-input stop, and no release or Lounge notice grants live action authority.

Release 0.16.1 keeps that host contract and loads as a compatible worker update. Exact-target viewport preparation now continues from first geometric visibility to proven pointer contact by centering the retained target within its nearest movable scroll surface when a sibling covers every candidate point. The recovery is bounded, revalidates after every movement, records only a categorical telemetry bit, and never dispatches through a cover. Require worker/current 0.16.1 with `restartRequired:false`; no additional MCP reconnect is needed after adopting 0.16.0.

Release 0.16.2 keeps that host contract and loads as a compatible worker update. Control inspection associates a bounded composite popup surface set with one exact owner, collapses nested wrappers, retains disjoint same-owner panels, and rejects surfaces with different owners before input. Selection and representation reconciliation use that same retained set. Require worker/current 0.16.2 with `restartRequired:false`; discard the failed inspection capability and do not replay any opener that may already have received input.

Release 0.17.0 changes the public Lounge catalog and MCP-host behavior while keeping worker protocol 13: host behavior 6, catalog 15, and 55 tools. Reconnect once, rejoin the same stable Lounge identity, require MCP/worker/current 0.17.0 with `restartRequired:false`, and read the automatically returned per-identity work note. Initialize or compare-and-set that note before waiting, then update it after every material work transition. The release also treats exactly one newly rendered popup as causal post-dispatch ownership only after a zero-surface baseline and one exact possible opener input; it does not weaken passive ambiguity or authorize replay.

Release 0.18.0 changes every public contract boundary: worker protocol 14, host behavior 7, catalog 16, and 56 tools. Reconnect once, rejoin the same stable Lounge identity, and require MCP/worker/current 0.18.0 with `restartRequired:false`. `browser_available_moves` now lists bounded current generic techniques and safe preparation paths from canonical command prerequisites without page semantics or authority expansion. Public `browser_open` never starts a stopped browser implicitly. When a fresh passive control inspection exposes one popup with a bounded current owner tie, the agent may explicitly declare the requested control's semantic ownership only through the returned structural gate; the proof is auditable and cannot override stale state, a competing owner, multiple surfaces, possible input, private boundaries, or authority. Discard all pre-reconnect capabilities and never replay historical openers or selections.

Release 0.19.0 keeps host behavior 7 and 56 tools, and moves to worker protocol 15 plus catalog 17 for the generalized popup-owner decision input. Reconnect once, rejoin the same Lounge identity, and require MCP/worker/current 0.19.0 with `restartRequired:false`. A passive ambiguous inspection may now return one-use opaque `ownerCandidateId` values for uniquely identified members of the complete bounded current set. The agent may choose one observed candidate—even when its role differs from the requested composite field—through `popupAssociation={owner:observed_candidate,ownerCandidateId,basis:agent_semantic_judgment}`. The controller revalidates the same document, requested control, unique candidate, one rendered popup, and association before later selection input. Discard every 0.18 capability. This contract authorizes no live observation or action and never permits replay.

Release 0.19.1 is compatible with the 0.19.0 host contract. Popup reveal prefers exact pointer contact and uses guarded native-button keyboard only as a definitively covered-target fallback under its authoritative popup reconciliation. Option selection remains pointer-only, and covered non-native controls remain blocked before input. Do not reconnect an adopted 0.19.0 host; require worker/current 0.19.1 with `restartRequired:false` at a safe boundary and discard the failed inspection plus every ref derived from it. Never replay the reported zero-dispatch opener, and do not infer live authority from a release or Lounge message.

Release 0.19.2 is also compatible with the 0.19.0 host contract. A zero-popup baseline followed by one possible exact opener input can causally bind several semantic roots only when they are partitions of one bounded positioned composed-tree envelope. Independent popup branches remain ambiguous, passive ambiguity is unchanged, raw rendered-surface count remains observable, and no opener is replayed. Do not reconnect an adopted 0.19.0 host; require worker/current 0.19.2 with `restartRequired:false`, discard the failed inspection and all derived refs, and start a fresh operation only inside the controlling thread's existing scope.

Release 0.19.3 keeps that host contract and adds a zero-input continuation for an already-open logical multi-root set. Stronger deterministic per-partition ownership still wins. Otherwise, one structurally proven common popup envelope may return bounded one-use owner candidates so the agent—not a label heuristic—chooses semantic ownership. Stage5 revalidates the exact current structure before retaining the surface set. Independent panels, stale decisions, and unbounded state remain closed. Require worker/current 0.19.3 with `restartRequired:false`; discard the prior failed inspection and never replay its opener.

Release 0.19.4 keeps the same host contract and closes the composite-node lifecycle gap found by the 0.19.3 actual-use gate. If the exact requested control is provably absent before input while one logical popup remains rendered, Stage5 can return current one-use owner candidates instead of requiring a whole-page role substitution. The agent chooses semantic equivalence; Stage5 re-resolves the surviving owner, binds the whole logical surface set, and retains that owner for later revalidation. A missing or changed popup fails with zero input and is never reopened through the decision. Require worker/current 0.19.4 with `restartRequired:false`; discard the failed inspection and every old candidate.

Release 0.19.5 changes MCP host behavior to 8 while retaining protocol 15, catalog 17, and 56 tools. Reconnect the MCP host once, rejoin the same stable Lounge identity, and require MCP/worker/current 0.19.5 with `restartRequired:false`; then discard every pre-reconnect ref, inspection, and owner-candidate ID. A missing requested composite control now exposes the complete bounded active structural proof-tier pool instead of allowing coverage or distance ranking to suppress semantically viable candidates. Deterministic evidence still bounds physical plausibility; the agent decides meaning through one current capability. Trace schema 2 self-identifies the loaded host contract and retains only categorical recovery facts: requested-control resolution, active/exposed/issued counts, truncation, judgment availability, and decision consumption. Candidate roles, names, IDs, geometry, and page content are never journaled. Default MCP prose is reduced from 5,846 to 338 words; results put outcome, dispatch, current popup/selection state, and a short next action before detailed evidence. The release authorizes no live observation or account action and never permits replay.

After reconnection, each task joins `stage5-lounge` with its assigned stable identity, resumes the returned work note, reads the pinned notice, sends one readiness message, and keeps `lounge_wait` pending whenever idle. A pending wait is what makes the active task wakeable; a message or notice revision wakes it, and after processing or an empty timeout it renews while collaboration is still active. Update the work note with compare-and-set revision and an idempotency key after every material start, completion, blocker, scope change, or next-action change. Browser-worker replacement preserves the MCP-side binding; `lounge_not_joined` means the current MCP connection is new or never joined and must call `lounge_join` again. A replacement connection with the same identity receives the exact note and supersedes the old writer. If the model task ends, messages and the note remain durable but cannot restart that model on their own. Manager access is disabled unless the trusted local server environment allowlists the identity with `STAGE5_LOUNGE_MANAGER_AGENT_IDS`; manager status may read current member notes. The complete workflow, initial identities, acknowledgement rules, and security boundary are in [agent-lounge.md](./agent-lounge.md).

## Runtime diagnosis

`browser_status` reports the MCP version, protocol version, host-behavior version, process start time, build fingerprint, tool-catalog version, compatible-update state, and `restartRequired`. Build fingerprints identify exact artifacts but are not compatibility contracts. Worker rebuilds that preserve the tool-catalog, worker-protocol, and host-behavior versions load automatically on the next browser operation, including a replacement needed while private handoff state is retained. Restart the MCP host only when `restartRequired` is true. `browser_recover` remains for failed browser workers; it is not an update mechanism.

Do not terminate an old-looking MCP, worker, or browser PID without proving ownership; multiple concurrent agents can legitimately have separate Stage5 Browser processes. Call `browser_available` before trial-starting backends. Its `profileState` distinguishes `startable`, `owned_active`, `owned_orphaned`, `busy_other_stage5_session`, and `external_owner` using the private atomic per-profile lease. Only the exact fingerprint-matched non-private orphan is automatically recoverable. A missing tool is a host-catalog problem; a present tool returning a structured launch failure is a runtime problem.

After any `browser_start` failure, call `browser_diagnostics` before retrying. It reports durable lease classification/heartbeat/control phase, the selected backend's executable preflight, profile writability and active lock files, sandbox policy, automation-control mode, observed `navigator.webdriver`, the last sanitized launch-failure category, and a safe suggested action. After an interaction failure, it reports bounded console/network categories and the most recent action's exact dispatch facts. Use `browser_execution_traces` with the exact operation ID to audit the owning manager, phase transitions, dispatch attempts, reconciliation conclusion, duration, and outcome. Raw browser errors, console or exception text, PIDs, credentials, page contents, URLs, selectors, names, coordinates, headers, bodies, query strings, fragments, form values, and full launch arguments are not included.

## File attachment model

`browser_snapshot` separately reports any HTML file inputs as `fileInputs`, including hidden inputs omitted from the ARIA tree. Each entry carries an opaque ref bound to that exact latest snapshot, frame, document version, and observed element. To attach a user-authorized local file, call `browser_set_input_files` with that `snapshotId`, ref, frame ID, and absolute path. The tool consumes the capability once, rejects symlinks/directories/unreadable files before dispatch, bypasses the native picker, confirms privacy-minimized name/size metadata during the capture-phase input event or from the retained `FileList`, and returns a fresh preview. This remains valid when a site consumes the event and immediately clears the input.

`observationMs` accepts 0–5,000 ms and is intended for a quick generic progress/network sample when no semantic completion expectation is supplied. For a known completion or error affordance, provide `completion` and use its `timeoutMs` for a bounded wait of up to 60,000 ms; the overall `timeoutMs` must be at least as large. If processing lasts longer, inspect the returned fresh snapshot later rather than replaying file selection.

Attaching is not posting, and file-input confirmation is not upload completion. Read `processing.state`: only `completion_observed` has explicit semantic progress or caller-supplied completion evidence. `in_progress`, `error_observed`, and `unverified` require inspection through the returned fresh snapshot before any submit action. A timeout or `file_selection_outcome_unknown` must never be replayed until a fresh observation proves the composer has no attachment. Absolute paths are neither returned nor journaled.

## Nested scrolling model

`browser_snapshot` also reports a bounded `scrollContainers` list for visible nested vertical scroll surfaces. Each opaque ref is bound to the exact latest snapshot, frame, document version, and observed element handle. Up to three in-viewport containers receive a bounded `# Visible semantic detail for scroll-*` subsection rooted at that exact surface, so deeply nested custom options can expose normal fresh refs without increasing whole-document depth. Detail refs are filtered to uniquely resolved, visible, in-viewport targets and use ordinary `browser_click_ref` no-replay semantics; always pair a state-changing option click with a bounded postcondition. When document scrolling does not move and `nested_scroll_containers_available` appears, take a fresh snapshot, choose the intended candidate by its privacy-safe label/role/geometry, and pass `{ snapshotId, ref }` through `browser_scroll.target`. If a popup's bounded detail stops before the intended choice, use one half-viewport step on that exact uniquely observed popup container, require confirmed movement, take a fresh snapshot, and discard every prior ref; repeat only through a newly observed capability and stop on ambiguity, unchanged geometry, boundary, timeout, or unknown dispatch. Each capability is consumed once. Never guess a selector, scroll the document instead of the popup, or replay a stale ref.

For dynamic feeds, `browser_scroll.waitFor` can wait for `article_count_growth`, `loading_indicators_disappear`, or `either`, bounded by its own timeout and the overall operation timeout. The returned `wait` reports only aggregate article/loading counts and explicit evidence. A timeout, remaining loader, or stable boundary is not a feed end. Scroll geometry tolerates a one-CSS-pixel difference so fractional values such as `2443.5 / 2444` are correctly recognized as the current boundary. `browser_diagnostics.page.lastAction` records `scroll`, and `lastActionNetworkEvents` contains only sanitized requests correlated with that scroll window.

## Private interaction and authentication model

The normal mode is one isolated, persistent Stage5 Browser profile per browser backend:

- Call `browser_auth_status` first. It reports profile/handoff state but deliberately returns `authenticated: "unknown"`; only the site's visible UI can prove that account state. Before a new login, explicitly select the intended backend instead of relying on a remembered default.
- If a password, passkey, CAPTCHA, OTP, EIN, identity document, selfie, or other private step is needed, call `browser_request_login_handoff`, optionally with its absolute URL. Stage5 Browser navigates while controlled, retains the exact `close_requested → process_exited → profile_unlocked` release phase, and launches the same isolated profile as a normal native browser without Playwright automation flags. A short request may first report `releasing_control`; call the same handoff request again to continue that phase, never to relaunch or repeat the private step. The ready response is `awaiting_user`, `browserConnected: false`, and `controlMode: "human_bootstrap"`.
- While the handoff is `awaiting_user`, do not call tab, page, action, start, switch, recover, or stop tools. They intentionally fail with `AUTH_HANDOFF_REQUIRED`; the agent cannot inspect or steer the private window.
- If a compatible build finishes during the private handoff, Stage5 Browser defers worker replacement. Resume through the existing worker first; the next operation rolls the proven same-process native session forward and restores its exact selected target. Do not reconnect or recover merely to load that compatible build.
- The native window contains a static Stage5 marker tab next to the private-action tab. The user should match that marker and the application name, then complete the sensitive step privately. Never ask them to paste a private value or send an identity document through the conversation.
- Follow the returned backend-specific instruction exactly. For Chromium, Chrome, Brave, and Edge, leave that application open and call `browser_resume_after_login`; Stage5 attaches to that same running process, preserving its in-memory session. For Firefox, quit the application normally, then resume while Stage5 waits within the remaining operation budget for the exact process to exit and the profile to unlock. On macOS, a persistent `.parentlock` file is ignored only when the OS confirms no process holds it. Do not use a bare origin such as `https://example.com` as the URL expectation; use a non-root post-login route or pass `null`.
- For bot-sensitive login or KYC, prefer Brave, Chrome, or Edge. The pinned Playwright Firefox binary currently reports `navigator.webdriver: true` in its uncontrolled native launch despite receiving no automation flags. Firefox keeps private values outside agent control and passes the shutdown/unlock/session-resume gate, but it does not currently promise automation invisibility.
- Chromium resume reports the actual runtime profile and privacy-safe storage at first attachment and after target load. Its fixed ephemeral CDP port is loopback-only and never appears in tool output. Compatible worker replacement disconnects and reconnects without closing the browser. Firefox retains the offline-after-exit checkpoint and bounded clean-exit override. Exact human clicks and private-window network traffic remain deliberately unobserved.
- Inspect `verificationPreview` immediately; if it still shows signed-out controls, stop. Then take a fresh full `browser_snapshot` and verify the site-specific account identity or signed-in affordance before acting. Storage continuity and a `lossBoundary` of `none` are evidence, not proof that the site accepted the login. Never request cookie values to resolve ambiguity.
- A unique visible dialog automatically becomes the snapshot root. Check `scope`, `visibleModalCount`, and `warnings`; this keeps portal/modal fields and buttons within the bounded depth while retaining valid refs from one ARIA capture. If `ambiguous_visible_modals` appears, do not guess which dialog to use.
- Persistent browser storage remains in that backend's Stage5 Browser profile across browser-worker, MCP-server, and agent-session restarts. Storage continuity is diagnostic evidence, never a claim that the site accepted or retained authentication.
- Chrome, Brave, Edge, Chromium, Firefox, and WebKit profiles do not share authentication with one another.
- Stage5 Browser never attaches to or imports cookies from the user's everyday browser profile.
- Tests and smoke checks use temporary profiles and do not modify the persistent profiles.

The handoff lifecycle is intentionally conservative:

```text
browser_stopped / profile_ready
              │ browser_request_login_handoff
              ▼
       releasing_control
              │ exact process exits and profile unlocks
              │ call the same request again only when instructed
              ▼
        awaiting_user
              │ native browser; no agent control
              │ Chromium: user finishes and leaves process open
              │ Firefox: user finishes and quits normally
              │ browser_resume_after_login
              ▼
ready_for_agent_verification
              │ fresh snapshot proves site state
              ▼
     normal semantic actions
```

The user normally signs in only once per backend profile. A later agent session can reuse that profile, but it must still inspect the current page instead of assuming authentication. Use an isolated backend that has never been authenticated when the task specifically needs an anonymous-user check.

Cookie import is intentionally not the login strategy: it expands credential exposure, can copy stale or device-bound state, and weakens the ownership boundary that makes recovery safe. Explicit profile reset and profile export/import are not exposed as agent tools.

Release 0.19.6 requires one MCP reconnect for host behavior 9; protocol 15, catalog 17, and 56 tools are unchanged. Rejoin the same Lounge identity, require MCP/worker/current 0.19.6 with `restartRequired:false`, and discard old refs. When a Chromium-family profile is already a proven native-CDP process, `browser_request_login_handoff` retains that exact process, selected target, document, and unsaved DOM while detaching automation and adding an exact Stage5 marker. Follow its instruction, leave the application open, and call `browser_resume_after_login` once. A `process_relaunch` result retains the older process-exit/profile-unlock lifecycle. Neither strategy grants new account authority or permits input replay.

Release 0.19.7 requires one MCP reconnect for host behavior 10; protocol 15, catalog 17, and 56 tools are unchanged. Rejoin the same stable Lounge identity, require MCP/worker/current 0.19.7 with `restartRequired:false`, and discard old refs. Native Chromium reattachment now gives the exact retained opaque target a bounded read-only settle reserve; it never substitutes a page by URL, title, or tab order and never emits input. `nativeReattach.resolution` reports `initial_exact`, `settled_exact`, `unresolved`, or `not_recorded` with privacy-safe counts and booleans. On `unresolved`, wait for tabs to settle and call `browser_start` once; if it repeats, stop and inspect the trace without selecting or replaying anything.
