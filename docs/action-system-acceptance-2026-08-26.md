# Stage5 Browser action-system acceptance

Current release: 0.21.4, tool catalog 23, worker protocol 21, MCP host behavior 16, 56 MCP tools.

This document is the privacy-safe replacement acceptance record for the discarded raw business-form dogfooding report. It records generic browser invariants, source owners, and disposable-fixture regressions only. No live account action, address, credential, tax value, payment value, document, query string, or private form value was used or retained.

## Product and architecture gate

Stage5 Browser is the agent's hand. Its useful unit is not a website-specific patch but a composable movement with truthful proprioception. The complete worker command surface is assigned exhaustively in `src/protocol/command-contracts.ts`; the complete 56-tool public surface, including host recovery and Lounge coordination, is assigned in `src/mcp/tool-contracts.ts` from the canonical names in `src/mcp/tool-names.ts`. Manager responsibilities, technique vocabulary, context layers, and the canonical action loop are declared in `src/protocol/capabilities.ts`.

Every consequential physical input follows:

```text
observe → plan → preflight → prepare → dispatch once → reconcile → finalize
                         └── one return to preflight only after proven zero input
```

Semantic choice belongs to the agent within the user's existing authority. Deterministic code owns structural identity, actionability, exact target binding, freshness, control mode, dispatch evidence, state reconciliation, privacy, and replay safety. When structural facts leave several current semantic candidates, the manager returns a bounded candidate set and may accept one explicit auditable agent judgment through a structural gate. That judgment never overrides stale state, missing authority, physical conflict, or no-replay evidence. Core code does not infer business meaning or a second private-value policy from labels, field types, URLs, regexes, scores, or site names.

The one controller runtime owns session context. Tab IDs, frame IDs, document versions, snapshots, control/form capabilities, exact handles, and action evidence have explicit narrower lifetimes. Only privacy-minimized ownership, terminal operation timing, page-lifecycle risk, transfer/dialog manifests, and Lounge coordination cross a durable boundary.

## Reported defect acceptance

| Finding | Governing invariant and implementation | Disposable regression evidence | Status |
| --- | --- | --- | --- |
| Unsaved form state disappeared around compatible worker replacement | Direct-Playwright replacement is deferred while connected. Proven native-CDP replacement reattaches the same browser and exact target. The private control record carries only opaque target/document identities, so a replacement during the detach gap emits durable `all_unsaved_form_state_may_be_lost` evidence before further work; an unavailable exact target fails instead of selecting another page. Same-document history is excluded. Owners: `src/supervisor/worker-lifecycle.ts`, `src/native-control-channel.ts`, `src/controller/lifecycle/native-attach.ts`, `src/controller/lifecycle/page-events.ts`. | `tests/supervisor.test.ts`; `tests/browser-controller/core/page-lifecycle.test.ts`; `tests/browser-controller/core/diagnostic-persistence.test.ts`; `tests/browser-controller/handoff/reattachment.test.ts` | Fixed and covered across attached and detach-gap replacement |
| Controller-selected page stayed hidden despite accepted activation | `browser_activate_selected_page` reconciles the exact selected renderer, document focus, and—only when required—the verified owned native application. An accepted activation request is never proof; unresolved visibility blocks input. Owners: `src/controller/observation/tab-activation.ts`, `src/controller/input/activation.ts`. | `tests/browser-controller-tab-capabilities.test.ts`; `tests/browser-controller/click/native-activation.test.ts`; `tests/browser-controller/click/role-activation.test.ts` | Fixed and covered headlessly; no native focus-changing smoke was run |
| A short internal deadline took minutes to reach the caller | Consequential work can be reserved before dispatch and queried without replay. Terminal creation, persistence completion, and response creation are retained in the operation registry; the supervisor enforces the outer wall-clock deadline. Owners: `src/operations/registry.ts`, `src/supervisor/execute.ts`, `src/mcp/context.ts`. | `tests/operation-registry.test.ts`; hard-deadline cases in `tests/supervisor.test.ts`; real stdio MCP deadline/recovery in `tests/mcp-operation-deadline.test.ts` | Fixed and covered end to end through MCP response creation |
| A click opened exactly one new page but only the original-page URL was checked | Click reconciliation reports `newPage`, `newPageCount`, and supports opt-in `expectedNewPageUrl`; multiple new pages remain ambiguous. Owners: `src/controller/action/click-executor.ts`, `src/controller/input/postconditions.ts`. | New-page case in `tests/browser-controller/click/postconditions.test.ts` | Fixed and covered |
| Date fill hid the required HTML format | Date fields fail before input with privacy-safe `invalid_date_format; expected YYYY-MM-DD`; an unambiguous ISO date is accepted. Owner: `src/controller/input/fill-evidence.ts`. | Date cases in `tests/browser-controller/core/fill-preparation.test.ts` | Fixed and covered |
| An exact autocomplete option lost its semantics between observation and dispatch | `browser_inspect_control` retains one-use exact control, popup, and option capabilities. For an exact editable combobox/searchbox, `browser_select_option(interaction=auto)` routes both direct targets and inspected option capabilities through one atomic query → exact active-descendant proof → one Enter on the control → selected-label/popup reconciliation; absent proof stops after the query without Enter or replay. Owners: `src/controller/controls/searchable-selection.ts`, `popup-association.ts`, `selection.ts`. | Direct and inspected-capability cases in `tests/browser-controller/core/searchable-selection.test.ts`; built-host coverage in `tests/mcp-form-fast-path.test.ts` | Extended in 0.21.2 |
| An offscreen/reactive popup opened after partial input while the action reported only failure | Partial trusted input enters reconciliation. If the exact requested visible/selected/hidden state is observed, the terminal result preserves partial dispatch evidence and reports the effect; otherwise it remains non-retriable. Owners: `src/controller/input/postconditions.ts`, `src/controller/action/click-executor.ts`. | `tests/browser-controller/click/popup-no-replay.test.ts`; `tests/browser-controller/click/target-replacement.test.ts` | Fixed and covered |
| A uniquely observed editor transiently returned zero matches | Role fill performs one bounded read-only re-resolution only while dispatch is proven false; ref fill remains bound to the retained exact handle and scope. No input is replayed. Owners: `src/controller/input/fill-role.ts`, `fill-ref.ts`. | Transient unique-editor case in `tests/browser-controller/core/fill-preparation.test.ts` | Fixed and covered |
| Worker replacement invalidated refs without workflow-level warning | Every replacement transition explicitly returns `allReferencesInvalid`; native reattachment preserves page state but not worker-owned capabilities. Durable page events separately identify document replacement and unsaved-state risk. Owner: `src/supervisor/worker-lifecycle.ts`. | `tests/supervisor.test.ts`; `tests/browser-controller/core/page-lifecycle.test.ts` | Fixed and covered |

## Missing-capability acceptance

| Requested capability | Generic implementation | Regression evidence | Status |
| --- | --- | --- | --- |
| Single select and multi-select | `browser_select_option(selected)` sets one exact native or custom option state in either direction; false requires positive current state and multi-select semantics. `browser_select_options` refreshes capabilities between additive physical selections, never toggles an already-selected option, and lets the agent preselect pointer or keyboard reveal for every internally required inspection. | `tests/browser-controller/core/control-options.test.ts`; `tests/browser-controller/core/control-option-state.test.ts`; built-host direct keyboard reveal in `tests/mcp-form-fast-path.test.ts` | Extended in 0.21.4 |
| Focused control inspection/list options | `browser_inspect_control` returns only the exact control's type/state and bounded options with opaque one-use IDs. The agent may preselect pointer or keyboard reveal; focus on or inside the exact semantic target and expanded evidence are evaluated before a global owner inventory without overriding another structural owner. Categorical target-first miss telemetry distinguishes insufficient focus, spatial mismatch, structural veto, and unavailable relations. | `tests/browser-controller/core/control-options.test.ts`; `tests/browser-controller/core/control-target-first-reveal.test.ts`; built-host coverage in `tests/mcp-form-fast-path.test.ts`; telemetry in `tests/execution-telemetry.test.ts` | Extended in 0.21.2 |
| Several viable pathways at a phase boundary | `browser_available_moves` derives bounded current techniques, prerequisites, enabling tools, costs, authority boundaries, and replay consequences from the canonical command contracts plus privacy-safe current capability counts. It never starts the browser or chooses business meaning. | `tests/available-moves.test.ts`; `tests/browser-controller/core/available-moves.test.ts`; `tests/mcp-available-moves.test.ts` | Implemented in 0.18.0 |
| Agent judgment for an unhardcodable semantic owner tie | One already-open rendered popup returns bounded current candidates with one-use opaque IDs. The agent may nominate any uniquely revalidated observed candidate—including a composite owner with a different ARIA role—without asking deterministic code to infer business meaning. The ID is bound to the document and requested control, sibling IDs are consumed together, and the chosen owner is revalidated before later option input. A resolved competitor, multiple surfaces, replaced document, stale capability, authority boundary, or possible-input state remains closed. | Requested-control and textbox-plus-button five-owner fixtures in `tests/browser-controller/core/control-popup-composition.test.ts`; changed-owner and one-use cases in `tests/browser-controller/core/control-popup-agent-judgment.test.ts`; categorical privacy case in `tests/execution-telemetry.test.ts` | Generalized in 0.19.0 |
| Redacted form state and staged fill | `browser_form_summary` returns labels/types/required-valid/value-presence plus structurally selected native-option labels, never free-form values. `browser_apply_form_plan` applies up to 20 exact steps and may rebind only an untouched field by one unique stable role/name/kind inside the retained original form after a same-document framework rerender. Completed or possibly dispatched steps are never replayed. | Same-form success and cross-form refusal in `tests/browser-controller/core/form-workflow.test.ts`; built-host coverage in `tests/mcp-form-fast-path.test.ts` | Extended in 0.21.0 |
| Field-scoped private handoff | Request/resume tools yield one exact observed field and return only completed/unchanged/validation-error state. Private values never return to agent-visible context or durable storage. | `tests/browser-controller/handoff/private-field.test.ts` | Implemented |
| Stable tabs and new-tab intent | Opaque session tab IDs, exact select/inspect/close, opener evidence, exactly-one-new-page reconciliation, and explicit expected-new-page URL replace index-only control. | `tests/browser-controller-tab-capabilities.test.ts`; `tests/browser-controller/click/postconditions.test.ts` | Implemented |
| Application-safe review mode | `normal` and `review_only` modes use deterministic command class plus agent-declared intent across clicks, key/pointer motions, fills, selections, checked state, and staged form plans. Submit, terms, account change, external communication, persistence, and financial intent can be blocked before dispatch without regex inference. | `tests/action-policy-manager.test.ts` | Implemented |

## Active Lounge regressions

| Reported failure | Generic remedy | Regression evidence | Status |
| --- | --- | --- | --- |
| Finance completed two attachment-processing operations, then three successful sidebar clicks navigated away while site persistence was unproven; returning showed the attachments gone | The transfer manager records only a bounded page-scoped file count after possible selection. Page summaries and upload results expose one compact `stateRisk`; processing completion explicitly remains distinct from workflow persistence. Interaction, navigation, and tab managers stop an agent-declared navigation before dispatch until the calling agent acknowledges that exact risk revision, then proceed normally without making the semantic decision. A later file selection resets acknowledgement. Exact native Chromium target/document binding carries the value-free state across worker replacement; document replacement clears it. | Built-MCP SPA upload/navigation and trace coverage in `tests/mcp-file-upload.test.ts`; manager revision behavior in `tests/page-state-risk-manager.test.ts`; exact native detach/reattach in `tests/native-worker-handoff.test.ts`; record validation in `tests/native-control-channel.test.ts` | Reproduced from Finance seq636 and privacy-safe operations `d8b53299-1b00-4424-9c3b-7fca7ad5a33d`, `b2624469-ee5a-465a-b306-e2a130f7f67e`, `ddd9a3a2-bfc9-404b-b7a1-79dec5297bd3`, `25784f79-2e58-433f-9933-4b70b2a1bae3`, `d13eb521-b891-4bd7-aafe-32ba9c4b3aec`, and `a04b308a-6171-4426-9fea-5f36d4c6dced`; fixed generically in 0.21.4; no live page or document was touched |
| Finance 0.21.2 direct multi-selection reached a pointer-sensitive opener after four reversible viewport moves; pointer-down/mouse-down replaced the control before click, and a later fresh snapshot found the correct popup open while the field remained empty | Preserve the operation as possible-input and non-retriable. Let `browser_select_options` carry an agent-chosen `revealInteraction=auto|pointer|keyboard` through its initial and every later fresh inspection. The canonical rendered-popup observer also waits read-only for at most 750 ms after possible opener input and classifies the proven effect as `immediate|stabilized`; it never dispatches again. | Built-MCP two-inspection keyboard fixture in `tests/mcp-form-fast-path.test.ts`; delayed partial-pointer and existing immediate/no-replay fixtures in `tests/browser-controller/core/control-reveal-recovery.test.ts`; categorical privacy coverage in `tests/execution-telemetry.test.ts` | Reproduced from operation `8eaf212e-cef2-4818-aa12-0e62b0c8e18d` and Finance seq635; fixed generically in 0.21.4. The live operation remains non-retriable, and current progress is explicitly controlling-user-gated rather than browser-blocked |
| Finance 0.21.1 successfully entered a country query, but an exact menuitem click chose guarded keyboard transport and a later Enter targeted the option; neither committed the active choice | In `auto`, route an inspected editable search-control option through the same atomic control commit as a direct target. Keep generic exact option-role clicks on pointer transport even with a postcondition; explicit `observed_option` preserves agent choice. Record `activationTransport=keyboard|pointer|mixed` categorically on traces with dispatch evidence. | Inspected-query commit in `tests/browser-controller/core/searchable-selection.test.ts`; button-backed menuitem pointer transport in `tests/browser-controller/click/popup-no-replay.test.ts`; privacy-safe transport extraction in `tests/execution-telemetry.test.ts` | Reproduced from operations `a6b6720e-6a5e-46a5-a9af-ac50e85ed9b4`, `3eb8e558-7c50-4e66-a98f-7c50fa17d23a`, and `adf38843-7d62-4276-8b8e-40b0efc2a4b4`; fixed generically in 0.21.2; prior possible input remains non-retriable |
| Finance 0.21.1 still fell through to the global owner cap after revealing a country menu because the active DOM node could live inside the exact semantic searchbox wrapper | Treat focus containment inside the retained exact semantic target as focused ownership only with spatial adjacency and no competing structural owner. A different structural owner also disables `post_dispatch_unique` causal fallback. Persist only a categorical `targetFirstMiss` reason. | Composite searchbox with 120 unrelated owners, plus a structurally owned competitor, in `tests/browser-controller/core/control-target-first-reveal.test.ts`; privacy-safe failure extraction in `tests/execution-telemetry.test.ts` | Reproduced from operation `9d9e5549-2c0d-4255-9b4d-87f1f88ede24`; fixed generically in 0.21.2; the live pointer action remains non-retriable |
| Finance 0.21.0 hit pointerDown-only control replacement on one role field, while a focused country field's adjacent popup was hidden behind a 100-plus global owner inventory | Preserve the partial pointer result as non-retriable. For fresh actions, let the agent choose keyboard reveal before dispatch and reconcile one Enter across control replacement. Resolve direct structural, focused, or expanded-plus-spatial evidence on the retained target before global inventory, but only after excluding a different structural owner. Retain categorical reveal-technique telemetry. | Pointer/keyboard replacement, 120 unrelated owners, and competing structural owner cases in `tests/browser-controller/core/control-target-first-reveal.test.ts` and `control-reveal-recovery.test.ts`; built MCP and telemetry in `tests/mcp-form-fast-path.test.ts` and `tests/execution-telemetry.test.ts` | Reproduced from operations `291dfc0b-a368-46cb-bf31-e256d9cb2c26` and `6675a45e-46ec-42fb-a608-5cdfa708af52`; keyboard and exact-element target-first foundation shipped in 0.21.1; composite-focus completion is recorded above; prior input remains non-retriable |
| One role-free surface remained ambiguous after normalized nearest-owner ranking because the intended exterior trigger and controls geometrically covered by the popup all had zero rectangle edge gap | Ownership preserves explicit/structural, focused, expanded, and spatial tiers. The spatial tier classifies material overlap through a bounded hit test: popup-covered siblings cannot defeat one tightly adjacent exterior anchor, while uncovered overlap, stronger-tier conflicts, and true exterior ties remain zero-input ambiguous. Portal partitioning still requires a real branch gap and keeps contiguous wrappers coalesced. | Separate/shared portal, contiguous wrapper, uniquely nearest anchor, popup-covered sibling, uncovered overlap, exact positional tie, and categorical telemetry cases in `tests/browser-controller/core/control-reveal-recovery.test.ts` and `tests/execution-telemetry.test.ts` | Live accepted on 0.15.8: one passive operation succeeded with five bounded candidates, two popup-covered siblings excluded, seven bounded options, and zero input; no selection or replay followed |
| An exact visible result was rejected as outside the viewport because a non-containing `overflow:hidden` ancestor collapsed inferred geometry | One canonical geometry evaluator keeps conservative overflow inference first, then accepts a viewport override only when bounded `elementFromPoint` probes inside the target's own viewport-clipped client rectangles hit that exact target or a composed-tree descendant. The synchronous dispatch guard repeats the same proof. | Positioned exact-target and existing covered/slotted/offscreen cases in `tests/browser-controller-hit-point.test.ts` | Fixed in 0.15.5 and live-accepted with one dispatch and passed URL postcondition |
| Nested scrolling made an exact retained option barely visible but every exact hit point remained under a sibling option, producing `target_covered_after_scroll` with zero dispatch despite more reversible movement being available | Continue the same bounded preparation loop while explicit hit testing remains false, move the retained target toward the nearest scroll surface interior, then repeat exact geometry and synchronous pre-input guards. No movement or no exact contact remains a zero-input stop. | Sticky sibling over a newly visible nested-surface target in `tests/browser-controller/click/modal-viewport.test.ts`, plus existing covered-target and action-phase telemetry cases | Reproduced from privacy-safe 0.16.0 phase telemetry and fixed generically in compatible worker 0.16.1; the live failed capability is never replayed |
| One exact custom control produced several rendered popup DOM surfaces, so a once-dispatched opener ended as `ambiguous_control_popup_after_reveal` even though the surfaces represented one widget | Associate surfaces by exact owner instead of raw DOM count. Collapse nested wrappers, retain disjoint same-owner panels as one bounded composite capability, collect and select across the set, and reject different owners before input. Preserve categorical rendered-count/owner telemetry when post-opener association still fails. | Nested competitor/target, split same-owner selection, different-owner zero-input, and post-opener telemetry cases in `tests/browser-controller/core/control-popup-composition.test.ts`, plus control rebinding/representation and execution-telemetry suites | Reproduced from privacy-safe 0.16.1 trace and fixed generically in compatible worker 0.16.2; the reported opener operation remains non-retriable and live resume is passive only |
| Preparation proved zero popup surfaces, one exact opener received possible input, and exactly one new rendered surface still failed because five static spatial owner candidates tied | Separate passive ownership from post-dispatch causality. Only a zero-to-one transition caused by that exact one-shot opener yields `post_dispatch_unique`; a pre-existing surface, zero/multiple new surfaces, or passive spatial tie remains closed. Preserve the categorical owner record without using it to erase causal evidence. | Five-candidate framework-replacement fixture plus existing passive tied-owner and different-owner zero-input cases in `tests/browser-controller/core/control-popup-composition.test.ts` | Reproduced from privacy-safe 0.16.2 trace and fixed generically in 0.17.0; operation `1b352135-079f-4750-a4a4-d61823ce5fb1` remains non-retriable and Coinbase stays frozen |
| A later fresh passive inspection of an already-open popup exposed five tied current owner candidates; deterministic geometry could not know which field the popup meant | Keep the zero-input ambiguity result, expose bounded current role/name and categorical owner evidence, and let the agent explicitly declare only the exact requested candidate in a fresh passive inspection. Record `agent_declared` categorically but omit candidate semantics from telemetry. Later selection remains a separate authorized action. | Finance-shaped five-candidate passive fixture in `tests/browser-controller/core/control-popup-composition.test.ts`, plus `tests/execution-telemetry.test.ts` | Fixed generically in 0.18.0; Finance remains frozen pending direct controlling-thread authority |
| The 0.18 passive gate still blocked a composite field because the requested textbox was not an owner candidate while a same-named button was one of five tied candidates | Do not hardcode textbox/button equivalence. Issue a one-use opaque ID for every uniquely named candidate in the bounded current set, let the agent choose from observed semantics, and revalidate the same document, requested control, one rendered popup, unique candidate, and declared ownership before any later selection dispatch. Consuming one ID invalidates its siblings; a changed candidate fails with zero input. | Finance-shaped composite success in `tests/browser-controller/core/control-popup-composition.test.ts`; changed-owner and stale-capability failures in `tests/browser-controller/core/control-popup-agent-judgment.test.ts`; public schema in `tests/mcp-scroll.test.ts` | Reproduced from privacy-safe 0.18.0 trace `ca7d1ad7-b6a4-4810-823f-9763adc6e7ca` and fixed generically in 0.19.0; the live page remains frozen until separately authorized passive re-observation |
| An ordinary custom popup opener was visible and enabled after nested scrolling but exact pointer contact remained under a pointer-active span, so `inspectControl` failed before dispatch even though its reveal manager owned an authoritative popup-open reconciliation | Make activation a required manager-owned plan policy. Popup reveal prefers pointer input and permits guarded native-button keyboard only as a fallback when hit testing is definitively false and its exact popup postcondition is authoritative. Option selection stays pointer-only because keyboard active-option semantics may target a different choice; covered non-native controls remain zero-input failures. | Finance-shaped covered native opener and covered non-native refusal in `tests/browser-controller/core/control-reveal-pointer-contact.test.ts`; partial-pointer no-replay in `control-reveal-recovery.test.ts`; button-option pointer semantics in `control-options.test.ts`; composed hit testing in `tests/browser-controller-hit-point.test.ts` | Reproduced from privacy-safe 0.19.0 trace `b98dd39d-0450-45ad-baa7-a19853ffa8dd` and fixed generically in compatible worker 0.19.1; the reported zero-dispatch operation is never replayed |
| A one-shot exact opener changed the page from zero rendered popup surfaces to seven semantic groups inside one logical popup, but raw surface count produced `ambiguous_control_popup_after_reveal`; after worker adoption the still-open set could not be inspected passively | Preserve raw count for telemetry. Under the zero-baseline/possible-dispatch causal gate, compose roots only when they share one bounded popup envelope and no root has a lower popup-like branch. For a pre-existing set, preserve stronger per-partition deterministic owners first; otherwise the same envelope may expose one bounded one-use semantic-owner decision for the agent, never automatic label inference. Independent panels, broad document containers, stale decisions, and unbounded sets remain closed. | Causal seven-group success, passive observed-owner success, and fixed-overlay/two-positioned-panel refusal in `tests/browser-controller/core/control-popup-causal-surface-set.test.ts`; broad-portal partition compatibility in `control-reveal-recovery.test.ts`; adjacent popup/reveal/selection matrix | Causal failure reproduced from 0.19.1 trace `3e714d96-a4f4-4961-8c3b-b1740735f6cd` and fixed in 0.19.2. Actual 0.19.2 snapshot `ade3fce9-fbf4-44b9-a619-e2f1ef025cc1` plus zero-input passive failure `e052e6ad-b238-4a9a-b9b9-1dd34fdfd21d` drove the 0.19.3 continuation fix; its actual-use gate exposed the disappearing composite-node boundary below before the broad suite |
| The exact textbox tuple that resolved during the 0.19.2 passive inspection returned `control_not_found` on 0.19.3 while the same logical popup remained open, so owner judgment was unreachable before popup association | After a proven zero-input missing/detached requested control, inspect only current rendered surfaces. If they form one logical popup, expose bounded supported-role owner candidates for agent judgment. Consume the one-use candidate against the original request, re-resolve the surviving owner, bind the whole logical set, and retain that owner as the canonical control. Never infer role equivalence or reopen a changed popup. | Missing textbox plus seven-group surviving-popup success and vanished-popup zero-input refusal in `tests/browser-controller/core/control-popup-causal-surface-set.test.ts`; stale decision and changed-owner coverage in `control-popup-agent-judgment.test.ts`; adjacent composition and selection-rebinding suites | Reproduced from privacy-safe 0.19.3 trace `d95d8d5f-9f42-4699-9877-1026a5f7e0a9` plus the reporter's sanitized byte-identical call classification and fixed generically in compatible worker 0.19.4; its actual-use gate exposed the five-to-one boundary below before the broad suite |
| The fresh 0.19.4 recovery returned one semantically wrong owner even though its trace showed five candidates in the active spatial tier, and the trace could not prove how many candidates were exposed or given capabilities | Preserve physical proof tiers as the positional bound, but expose their complete bounded pool whenever target meaning is unresolved. Coverage and distance remain diagnostics rather than semantic suppression. Retain requested-control state, active/exposed/issued counts, truncation, judgment availability, and decision consumption in a self-identifying host trace without candidate semantics. | Five-candidate missing-textbox and seven-group zero-input recovery in `tests/browser-controller/core/control-popup-causal-surface-set.test.ts`; fresh-host trace/privacy coverage in `tests/mcp-scroll.test.ts`; allowlist coverage in `tests/execution-telemetry.test.ts` | Actual operation `78cad255-376b-4103-b4fc-14abfbb1e107` proved the five-to-one collapse with zero input. Fixed generically in 0.19.5/host behavior 8; Finance verified the exact loaded contract and resumed ordinary authorized work without an artificial dropdown acceptance exercise |
| Repeated lifecycle/safety essays on every MCP tool made an agent service the abstraction instead of using ordinary UI judgment, while a successful multi-select with its menu intentionally open buried that state in low-level evidence | Keep enforced safety in managers, not repeated prompt text. Bound server instructions plus tool titles/descriptions to 10% of the measured legacy baseline. Successful selections lead with outcome, dispatch, `selectionSucceeded`, current selected/popup/multiple state, and a short next-action enum while retaining full evidence afterward. | Actual catalog measurement in `tests/mcp-scroll.test.ts`; ordering in `tests/mcp-result-shaping.test.ts`; persistent multi-select popup in `tests/browser-controller/core/control-selection-representation.test.ts` | Agent feedback reproduced the reasoning bottleneck. 0.19.5 reduces default prose from 5,846 to 338 words (94.2%) and reports the intentional open-menu state directly; safety and no-replay behavior are unchanged |
| A verified native-CDP Brave handoff closed its Playwright context, then spent every deadline waiting for that healthy profile-owning process to exit; a later compatible-worker transition reported `pageStatePreserved=false` and the live form was empty | Make release strategy explicit. Direct Playwright and Firefox retain close → exact process exit → profile unlock. A proven native-CDP process instead receives an exact Stage5 marker, drops only its automation connection, changes its durable record to `awaiting_user`, and retains the same process/profile/target/document. Recover either record/lease transition side only from exact process plus marker proof. Defer compatible updates from the durable private-handoff lease even when the host's in-memory connection bit is false; do not claim unobservable tab-strip adjacency. | Pre-fix orphan adoption, same-process handoff, unsaved draft, marker, transition-crash recovery, and resume in `tests/browser-controller/handoff/native-same-process.test.ts`; durable close-requested update deferral in `tests/supervisor.test.ts`; fresh-host privacy trace in `tests/mcp-native-private-handoff.test.ts` | Reproduced from Finance operations `a1ea68c8-efd3-41a5-a4e1-aeb66e840d13`, `b83cf51f-30bb-46ca-8554-d74bcf857c50`, and `ad2f872e-64bb-45b3-acd0-591c47f2912b`, then validated against Finance's canonical non-preserving transition; fixed generically in 0.19.6/host behavior 9 without touching the live account or process |
| A fresh exact 0.19.6 Chrome worker twice failed native start with `selected_page_unavailable_after_reattach`, while a later privacy-safe probe proved the recorded target still existed among six healthy pages | Give only exact opaque target discovery a 750 ms read-only settle reserve. Repeat live-page inventory and exact CDP identity observation without URL/title/order fallback, navigation, tab selection, or browser input; unresolved identity still fails closed. Retain only bounded categorical settle proprioception so no ad-hoc probe is needed next time. | Transient first-probe miss in `tests/browser-controller/handoff/reattachment.test.ts`; direct privacy allowlist in `tests/execution-telemetry-native-reattach.test.ts`; fresh-host durable trace in `tests/mcp-native-reattach-telemetry.test.ts`; adjacent handoff/supervisor/version gates | Reproduced from Twinkle operations `598397ee-a276-4849-ada7-99578ee6e6d6` and `8b3abbb0-dd1a-4f9f-899d-41aa297f94ab`; fixed generically in 0.19.7/host behavior 10 with zero live browser input |
| A Finance dropdown applied one exact selection but replaced its React control, left its multi-select open, and consumed 14,301 ms in failed reconciliation; repeated full traces/snapshots and mandatory private-value prose then consumed agent context | Observe selected representation from the retained field across control replacement, permit one read-only exact rebind, and cap this proof phase at 1,500 ms without replay. Lead with selection state and viable next motions. Make task snapshots, traces, Lounge status, and unchanged waits compact by default. Treat value entry as an upstream user/model-policy decision while structurally excluding values from every observation and durable surface. | Replacement/open-popup regression in `tests/browser-controller/core/control-selection-representation.test.ts`; form privacy in `form-workflow.test.ts`; telemetry filter/privacy in `execution-telemetry.test.ts` and `mcp-scroll.test.ts`; Lounge compact/full gates; `snapshot-view.test.ts`; server prose measurement | Actual operation telemetry preceded implementation. Fixed generically in 0.20.0/host behavior 11/protocol 16/catalog 18; no live account action, native focus change, or historical replay was used |
| A compatible worker artifact rebuild made the old connected worker reject its next passive command even though the supervisor intentionally deferred replacement to preserve page state | Build fingerprints are diagnostic identities, not compatibility contracts. The worker self-monitor keeps serving across a fingerprint-only rebuild and advertises the compatible update; only a changed worker protocol contract disconnects. The supervisor remains the sole owner of the safe replacement boundary. | Compatible-rebuild and protocol-change cases in `tests/runtime-info.test.ts`; connected direct-Playwright deferral in `tests/supervisor.test.ts` | Reproduced from Twinkle operation `a973e6fe-c081-4850-82b8-c55b8519dfe1` and fixed in 0.20.0 without browser input |
| A once-dispatched expansion succeeded, but duplicate exact hidden controls made `expectedHidden` unknown | Zero matches or every match in the bounded exact semantic set is objective hidden proof; any visible match fails, and an over-limit or failed observation remains unknown. | Duplicate-all-hidden success in `tests/browser-controller/click/postconditions.test.ts`; multiple-visible failure in `tests/browser-controller/click/role-activation.test.ts` | Reproduced from 0.15.5 trace and fixed in compatible 0.15.6 candidate; the original click is never replayed |
| Agent narratives alone could not localize popup, ownership, viewport, option-state, candidate-handoff, native target-discovery, or locked-profile boundaries | Execution traces self-identify their schema and loaded host contract, then retain categorical target state/viewport proof, popup association/surface proof, rendered-surface count, owner tier/candidate classes/decision, requested-control resolution, active/exposed/issued candidate counts, truncation, judgment availability/consumption, reach strategy, exact-target settle resolution, profile ownership proof/liveness/match/control phase, dispatch evidence, reconciliation outcome, and boolean desired/observed state through an explicit privacy allowlist. Any new host-owned trace field increments the host behavior contract. | `tests/execution-telemetry.test.ts`, `tests/execution-telemetry-native-reattach.test.ts`, fresh-host MCP acceptance in `tests/mcp-scroll.test.ts` and `tests/mcp-native-reattach-telemetry.test.ts`, and native-record validation in `tests/native-control-channel.test.ts` | Core telemetry live-verified on 0.15.5/host behavior 2; owner evidence required host 3, timeout handoff host 4, option direction host 5, candidate handoff host 8, private handoff host 9, exact native discovery required host 10, and locked-profile ownership evidence requires 0.20.0/host behavior 11 |
| A custom option selection consumed its full deadline before entering the action manager, and hard worker recovery left the durable trace with no phase or dispatch evidence | Baseline representation uses the exact retained control handle under a 500 ms no-input bound. Worker shutdown snapshots completed and active owning-manager phases, and the host retains the late response only for the exact timed-out request; possible dispatch remains unknown and non-retriable. | `tests/browser-controller/core/control-selection-timeout.test.ts`, `tests/action-phase-manager.test.ts`, `tests/supervisor-timeout-telemetry.test.ts`, and fresh-host `tests/mcp-operation-deadline.test.ts` | Fixed in 0.15.10/host behavior 4; the historical selection remains frozen and is never replayed |
| One trusted custom-option click produced no ARIA selected state while the site intentionally kept its multi-select popup open and rendered the selected chip outside the control's old local wrapper | Retain an adaptive exact field region up to the first competing-control boundary, exclude the owned popup, and compare its exact rendered representation against a pre-dispatch baseline. Multi-select requires selected state or a new field-local representation; popup closure alone never passes, and an unavailable visibility observation remains unknown. | Four open-popup, cross-field isolation, closure-only, and observation-timeout cases in `tests/browser-controller/core/control-selection-representation.test.ts`, plus existing control and telemetry suites | Fixed generically in compatible worker 0.15.11; the reported trusted click remains non-retriable and no live account was used to validate the fix |
| A long custom-control inspection retained an opener that the framework replaced in the same document; selection then failed before dispatch and before entering the action manager | Probe the retained opener under a strict read-only bound. Only a proven detached handle permits one exact role/name control re-resolution plus one uniquely associated rendered-popup rebind; acquire the field baseline inside the owning action observe phase, then preserve the one-dispatch/no-replay gate. Unknown or ambiguous state fails closed. | Framework replacement, stalled retained handle with phase telemetry, already-satisfied zero-dispatch, and prior representation cases in `tests/browser-controller/core/control-selection-representation.test.ts` and `tests/browser-controller/core/control-selection-timeout.test.ts` | Fixed generically in compatible worker 0.15.12; both reported live operations remain non-retriable and no live account was used to validate the fix |
| A fresh 0.15.12 control inspection retained a popup node that the framework replaced while leaving the exact control connected | Treat control and popup capabilities as independent positional state. A proven non-rendered popup permits one read-only reassociation only to one newly rendered popup uniquely owned by the exact current control; never replay the opener, and retain unknown/missing/ambiguous state as zero-input failure. | Separate control replacement, popup replacement, closed-popup no-opener-replay, categorical recovery telemetry, and existing timeout/representation cases in `tests/browser-controller/core/control-selection-rebinding.test.ts` and related suites | Fixed generically in compatible worker 0.15.13; the reported inspection and selection remain non-retriable and no live account was used to validate the fix |
| A fresh 0.15.13 inspection proved an already-open uniquely owned popup, but the following exact selection sampled the popup during another framework replacement and failed after 206 ms despite an unused two-second observe budget | Treat popup identity as dynamic positional state until the dispatch gate. Within the existing observe deadline, passively re-resolve through short missing/hidden replacement intervals; require a rendered uniquely owned surface, exclude hidden retained surfaces from selection association, retain closed/ambiguous state as zero-input failure, and never replay its opener. | Replacement-gap, hidden-retained-versus-rendered-portal, control replacement, popup replacement, and closed-popup cases in `tests/browser-controller/core/control-selection-rebinding.test.ts`, plus control/action/telemetry suites | Reproduced deterministically and fixed generically in compatible worker 0.15.14; the live inspect and zero-dispatch selection remain non-retriable and Coinbase stayed frozen |
| A custom `role=option` stored its authoritative state in a nested checkbox, intentionally kept its multi-select popup open, and exhausted reconciliation after one dispatched click | Use one bounded exact-option state evaluator for inspection and reconciliation. Read explicit native, ARIA, and framework state on the option or its descendants; propagate checkbox evidence as a multi-select signal; reject conflicting state and ignore CSS appearance. An already-selected option never receives a toggle. | Nested unchecked/checked state, persistent popup, framework state, and appearance-only class cases in `tests/browser-controller/core/control-option-state.test.ts`, plus existing control, form, phase, and telemetry suites | Reproduced from the 0.15.14 durable trace and fixed generically in compatible worker 0.15.15; the reported one-click operation remains non-retriable and no live account was used to validate the fix |
| A passive 0.15.15 snapshot exposed three selected field chips while inspection reported the exact stateless option rows as `selected:null` and the control as single-select | Inventory every unique observed option name in one canonical field-local representation pass, excluding the popup and competing fields. Map only positive exact evidence, infer multi-select from several mapped choices, retain that scope, revalidate a represented choice through action phases with zero dispatch, and reject explicit-state conflicts before input. | Existing-chip mapping, multi-select inference, cross-field isolation, zero-toggle phase evidence, and conflict cases in `tests/browser-controller/core/control-option-state.test.ts`, plus existing representation/control/telemetry suites | Reproduced deterministically from the zero-input 0.15.15 report and fixed generically in compatible worker 0.15.16; no live correction or action was used for validation |
| The live composite field placed generic short selected chips above a same-named internal textbox, so 0.15.16's narrow scope mapped nothing; the public hand also lacked an exact inverse option motion | Expand past competing fields only when every field shares the opener's structural name, map a short chip only to one unique longer option, and make `browser_select_option(selected)` a desired-state primitive. Deselect requires positive current state plus multi-select semantics, dispatches once, and reconciles explicit false or exact representation disappearance. Direction is retained as privacy-safe boolean telemetry. | Composite scope, unique/ambiguous prefix, zero-dispatch, custom and checkbox-backed deselection, unknown-state refusal, and native peer-preservation cases in `tests/browser-controller/core/control-option-state.test.ts`, `control-options.test.ts`, and `tests/execution-telemetry.test.ts` | Reproduced from sanitized zero-input facts and fixed generically in 0.16.0; no live account or native-focus action was used |

## Complete hand vocabulary

The current generic families cover lifecycle, bounded perception, current-move planning, URL/history navigation, exact tabs, click/ref action, hover, focus, key press, double-click, context-click, drag-and-drop, vertical/horizontal document and nested scrolling, text/date fill, checked state, desired single/multi-option state, staged form work, observed file input, upload-state evidence, durable download capture, expected/unexpected JavaScript dialogs, screenshots, field/authentication private handoff, review policy, operation recovery, durable page lifecycle, and agent coordination.

This does not promise a bespoke command for every future widget. It promises that ordinary variation composes from these techniques, and that a genuinely missing motion is added once at the generic manager boundary with a disposable regression—not patched per site.

## Validation gate

Focused regressions run without rebuilding unchanged code. The complete release suite remains serial, gives each run an automatically removed root under `/private/tmp/stage5-browser-tests.noindex`, and bounds fixture teardown with exact disposable-process proof so indexing or one delayed browser exit cannot contaminate later tests. Seventeen abandoned directories from the old per-user temp runner (46 MB) were verified process-free and removed.

Final 0.13 post-change release evidence:

- `npm test`: build, typecheck, file-size gate, and complete headless suite passed in 140.43 seconds;
- test files: 63 passed, 3 skipped, 66 total;
- tests: 224 passed, 3 skipped, 227 total;
- all production TypeScript files are 497 lines or fewer; all hand-authored TypeScript files remain below the enforced 1,000-line ceiling;
- public MCP catalog and exhaustive manager-owner contract: 53 unique tools;
- native focus-changing, native handoff smoke, and live-account tests were deliberately excluded because their boundaries were not required for this release.

Later additive evidence is recorded in the corresponding dogfooding records; it does not replace or reinterpret the historical 0.13 gate above. The final post-correction 0.18.0 and 0.19.0 release gates are recorded below.

Final 0.18.0 release evidence:

- focused changed-boundary gates: 10 files/31 tests passed; probe-driven planner corrections then passed 13 and 14 tests across 2 files;
- definitive post-correction `npm test`: build, typecheck, file-size gate, and the complete headless suite passed in 204.43 seconds;
- test files: 82 passed, 3 skipped, 85 total;
- tests: 302 passed, 3 skipped, 305 total;
- public MCP catalog and exhaustive manager/technique/prerequisite-owner contract: 56 unique tools;
- production TypeScript remains at or below the roughly 500-line target and every hand-authored TypeScript file remains below the enforced 1,000-line ceiling;
- native focus-changing, native handoff smoke, and live-account probes were deliberately excluded because those boundaries were not changed.

Final 0.19.0 release evidence:

- focused final gates passed 9 tests across the public-schema/composite-owner files, 11 tests across the planner/candidate-lifetime files, and 2 adversarial candidate-binding tests;
- definitive post-correction `npm test`: build, typecheck, file-size gate, and the complete headless suite passed in 262.14 seconds;
- test files: 83 passed, 3 skipped, 86 total;
- tests: 305 passed, 3 skipped, 308 total;
- public MCP catalog and exhaustive manager/technique/prerequisite-owner contract: 56 unique tools, catalog 17, worker protocol 15, and host behavior 7;
- production TypeScript remains at or below 497 lines and every hand-authored TypeScript file remains below the enforced 1,000-line ceiling;
- native focus-changing, native handoff smoke, and live-account probes were deliberately excluded because those boundaries were not changed.

Final 0.19.1 release evidence:

- the Finance-shaped covered-popup regression failed before the implementation at `target_covered_after_scroll` with zero dispatch, then passed with one exact guarded native keyboard activation and authoritative popup reconciliation;
- a covered non-native control remained blocked with zero dispatch, partial-pointer popup recovery remained no-replay, and button-shaped option selection remained pointer-only;
- 58 focused control, hit-test, recovery, action-phase, and telemetry tests passed across 10 files after the activation policy was narrowed;
- definitive `npm test`: build, typecheck, file-size gate, and the complete headless suite passed in 275.03 seconds;
- test files: 84 passed, 3 skipped, 87 total;
- tests: 307 passed, 3 skipped, 310 total;
- public MCP catalog remains 56 tools, catalog 17, worker protocol 15, and host behavior 7;
- production TypeScript remains at or below 497 lines and every hand-authored TypeScript file remains below the enforced 1,000-line ceiling;
- native focus-changing, native handoff smoke, and live-account probes were deliberately excluded because those boundaries were not changed.

Final 0.19.5 release evidence:

- actual Finance and agent-overload feedback preceded implementation and regression work; Finance later verified exact 0.19.5/host8/protocol15/catalog17/56 with `restartRequired:false` and resumed normal authorized work without a synthetic dropdown gate;
- default server instructions plus tool titles/descriptions measured 338 words versus the 5,846-word legacy baseline, a 94.2% reduction under the enforced 584-word ceiling;
- build, typecheck, file-size, diff, fresh-host MCP, candidate-pool, telemetry-privacy, result-ordering, and persistent multi-select focused gates passed;
- definitive post-correction `npm run test:focused` against the already-built revision passed the complete headless suite in 242.56 seconds;
- test files: 85 passed, 3 skipped, 88 total;
- tests: 313 passed, 3 skipped, 316 total;
- public MCP catalog remains 56 tools, catalog 17, worker protocol 15, and host behavior 8;
- production TypeScript remains at or below 497 lines and every hand-authored TypeScript file remains below the enforced 1,000-line ceiling;
- native focus-changing, native handoff smoke, and live-account probes were deliberately excluded because those boundaries were not required.

Final 0.19.6 release evidence is recorded in `docs/dogfooding-2026-08-27-native-same-process-handoff.md`. The complete serialized headless gate passed in 221.41 seconds: 87 test files passed and 3 skipped; 317 tests passed and 3 skipped. Production TypeScript is at most 497 lines. The catalog remains 56 tools, catalog 17, protocol 15, and host behavior changes to 9 for the new privacy-safe handoff trace allowlist.

Final 0.19.7 release evidence is recorded in `docs/dogfooding-2026-08-27-native-target-settle.md`. The complete serialized headless gate passed in 221.00 seconds: 89 test files passed and 3 skipped; 320 tests passed and 3 skipped. Production TypeScript is at most 497 lines. The catalog remains 56 tools, catalog 17, and protocol 15; host behavior changes to 10 for the privacy-safe exact-target discovery conclusion.

Final 0.20.0 release evidence is recorded in `docs/dogfooding-2026-08-27-hand-contract-and-fast-reconciliation.md`. Actual Finance and Twinkle telemetry preceded implementation. Focused changed-boundary gates passed 20 files/66 tests, followed by 3 files/15 tests after the final observation and module split, then 4 files/6 tests after compact snapshots were hardened to discard omitted ref capabilities. The definitive serialized `npm test` passed build, typecheck, file-size enforcement, 90 test files and 328 tests in 209.94 seconds; 3 platform/native-smoke files and 3 tests were skipped. The public catalog remains 56 tools and moves to catalog 18, protocol 16, and host behavior 11. Production TypeScript is at most 500 lines and every hand-authored TypeScript/JavaScript file remains below 1,000 lines. Native focus-changing, private handoff smoke, and live-account tests were deliberately excluded because those boundaries were not required for this release.
